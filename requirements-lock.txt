# Exact Python package versions the committed results in proteomics_de/results/
# were produced with (Homebrew CPython 3.13.7, macOS arm64).
#
# This is a RECORD, not an install manifest for a fresh machine: the packages
# live in the Homebrew site-packages and are inherited by the repo-local venv via
# `python3 -m venv --system-site-packages .venv`. See ENVIRONMENT.md.
#
# Versions below were read from the actually-imported modules, not from a
# pip freeze of an isolated environment.
#
# Numeric output (limma moderated t-statistics, PCA coordinates, GSEA ES) is
# sensitive to the BLAS/LAPACK build behind numpy/scipy as well as to these
# version pins. Tests marked `golden` assume this exact stack.
#
# R-side dependencies are NOT listed here. They are recorded in
# proteomics_de/_limma_versions.txt: R 4.6.1, limma 3.68.4, imputeLCMD 2.1.

pandas==2.3.3
numpy==2.3.5
scipy==1.16.3
scikit-learn==1.7.2
pandera==0.32.1
matplotlib==3.10.7
seaborn==0.13.2
requests==2.32.5
networkx==3.6
gseapy==1.1.11
upsetplot==0.9.0
adjustText==1.3.0
openpyxl==3.1.5
PyYAML==6.0.3
